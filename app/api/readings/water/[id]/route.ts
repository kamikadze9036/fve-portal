import { eq } from 'drizzle-orm';
import { NextResponse } from 'next/server';
import { getDb, serializeWrite } from '@/db';
import { auditLog, waterMeterReadings } from '@/db/schema';
import { dateValue, nonNegative, optionalText } from '@/lib/readings-validation';
const idOf = (v: string) => { const id = Number(v); if (!Number.isInteger(id) || id < 1) throw new Error('Neplatné ID odečtu.'); return id; };
export async function PATCH(request: Request, { params }: { params: Promise<{ id: string }> }) { try { const id = idOf((await params).id); const input = await request.json() as Record<string, unknown>; const [current] = await getDb().select().from(waterMeterReadings).where(eq(waterMeterReadings.id, id)); if (!current) return NextResponse.json({ error: 'Odečet nebyl nalezen.' }, { status: 404 }); const updates = { measuredAt: input.measuredAt === undefined ? current.measuredAt : dateValue(input.measuredAt), mainMeterM3: input.mainMeterM3 === undefined ? current.mainMeterM3 : nonNegative(input.mainMeterM3, 'Hlavní vodoměr', true)!, gardenMeterM3: input.gardenMeterM3 === undefined ? current.gardenMeterM3 : nonNegative(input.gardenMeterM3, 'Zálivkový vodoměr', true)!, note: input.note === undefined ? current.note : optionalText(input.note) }; const row = await serializeWrite(async () => { const next = (await getDb().update(waterMeterReadings).set(updates).where(eq(waterMeterReadings.id, id)).returning())[0]; for (const key of ['measuredAt', 'mainMeterM3', 'gardenMeterM3', 'note'] as const) if (String(current[key] ?? '') !== String(updates[key] ?? '')) await getDb().insert(auditLog).values({ tableName: 'water_meter_readings', recordId: id, fieldName: key, oldValue: current[key] == null ? null : String(current[key]), newValue: updates[key] == null ? null : String(updates[key]) }); return next; }); return NextResponse.json(row); } catch (error) { return NextResponse.json({ error: error instanceof Error ? error.message : 'Odečet se nepodařilo upravit.' }, { status: 400 }); } }
export async function DELETE(_: Request, { params }: { params: Promise<{ id: string }> }) { try { const id = idOf((await params).id); const [row] = await serializeWrite(() => getDb().update(waterMeterReadings).set({ qualityStatus: 'nahrazené' }).where(eq(waterMeterReadings.id, id)).returning()); return row ? NextResponse.json(row) : NextResponse.json({ error: 'Odečet nebyl nalezen.' }, { status: 404 }); } catch (error) { return NextResponse.json({ error: error instanceof Error ? error.message : 'Odečet se nepodařilo archivovat.' }, { status: 400 }); } }
